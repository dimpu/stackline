import { css } from "@emotion/css";

export const infoClassName = css({
  marginBottom: 10,
});

export const productImgClassName = css({
  width: 150,
});
export const tagClassName = css({ marginTop: 10 });
